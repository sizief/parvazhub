<% if user_signed_in? %>
    <%= link_to(current_user.email, edit_user_registration_path, class:"menu-links") %>
<% else %>
  <%#= link_to('ثبت‌نام', new_user_registration_path, class:"menu-links")  %>
<% end %>